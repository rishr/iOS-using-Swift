import Foundation

/* Replace with CoreData
class Schedule {
    let id: String
    var arrival, departure: Date
    var stops: [Stop]
    
    init(id: String, arrival: Date, departure: Date, stops: [Stop]) {
        self.id = id
        self.arrival = arrival
        self.departure = departure
        self.stops = stops
    }
}
*/
