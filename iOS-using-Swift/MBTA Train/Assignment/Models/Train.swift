import Foundation

/* Replace with CoreData
class Train {
    let id: String
    var name, source, destination: String
    var schedules: [Schedule]
    
    init(id: String, name: String, source: String, destination: String, schedules: [Schedule]) {
        self.id = id
        self.name = name
        self.source = source
        self.destination = destination
        self.schedules = schedules
    }
}
*/
